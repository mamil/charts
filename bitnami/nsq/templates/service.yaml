apiVersion: v1
kind: Service
metadata:
  name: {{ include "nsq.fullname" . }}
  labels:
    {{- include "nsq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nsq.selectorLabels" . | nindent 4 }}
